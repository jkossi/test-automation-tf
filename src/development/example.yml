workspace_example_data: "{{ example_data }}!!!"
more_data: "ok"

sources:
  - name: mySource
    type: ruby

transformations:
  - name: me
    type: js
    script: |
      var allowedStuff = [1,2,3];
      if (event.test in allowedStuff) return;
      return event;
      
destinations:
  - name: my_destination
    transformation: me
